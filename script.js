// Guava Bob's Grande Adventures - Website Scripts
document.addEventListener('DOMContentLoaded', () => {
    console.log("Welcome to Guava Bob's Grande Adventures!");
    
    // Smooth scrolling for navigation if we had anchor links
    // Currently, we have separate pages, so JS is minimal.
});
